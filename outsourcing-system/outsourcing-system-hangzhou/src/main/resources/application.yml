server:
  port: 10003

spring:
  application:
    name: outsourcing_system_hangzhou

---
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/customer_hangzhou?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8
    username: root
    password: a1b2c3d4
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
